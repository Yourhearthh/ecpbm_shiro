
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ecpbm_shiro?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=PRC
    username: root
    password: 123456
    # password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    auto-mapping-behavior: full
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    mapper-locations: classpath*:mapper/**/*Mapper.xml
server:
  port: 8090
